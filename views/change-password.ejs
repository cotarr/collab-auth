<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Change User Password</title>
    <link rel="stylesheet" href="/css/password.css">
  </head>
  <body>
    <form action="/changepassword" method="post">
      <fieldset>
        <div class="center">
          <h1>Change Password</h1>
        </div>
        <p>Username: <%= username %></p>
        <div hidden>
          <input name="username" type="text" value="<%= username %>" autocomplete="username" readonly>
        </div>
        <div>
            <label for="oldpassword">Current password:</label>
            <input id="oldpassword" type="password" name="oldpassword"
            minlength="1" maxlength="<%= opt.maxPwLen %>"
            autocomplete="current-password" required />
        </div>
        <div>
            <label for="newpassword1">New password:</label>
            <input id="newpassword1" type="password" name="newpassword1"
            minlength="<%= opt.minPwLen %>" maxlength="<%= opt.maxPwLen %>"
            autocomplete="new-password" required />
        </div>
        <div>
            <label for="newpassword2">New password again:</label>
            <input id="newpassword2" type="password" name="newpassword2"
            minlength="<%= opt.minPwLen %>" maxlength="<%= opt.maxPwLen %>"
            autocomplete="new-password" required />
        </div>
        <div>
          <button class="button" type="submit" value="Submit">Submit</button>
          <a href="/changepassword?cancel=yes"><button class="button lm-20" type="button">Cancel</button></a>
        </div>
      </fieldset>
    </form>
  </body>
</html>
